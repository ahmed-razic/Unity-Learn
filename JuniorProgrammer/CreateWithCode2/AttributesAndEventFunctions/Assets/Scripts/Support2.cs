using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Support2 : PlayerController
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(age);
        Debug.Log(city);
    }
}
